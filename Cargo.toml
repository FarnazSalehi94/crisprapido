[package]
name = "crisprapido"
version = "0.1.0"
edition = "2021"

[features]
debug = []

[dependencies]
clap = { version = "4.4", features = ["derive"] }
bio = "1.3"
libwfa2 = "0.1.1"
rand = { version = "0.8", features = ["small_rng"] }
rayon = "1.8"
